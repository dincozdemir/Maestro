{
  "name": "extractPoint",
  "description": "Extracts center point from an image based on a given query",
  "strict": true,
  "schema": {
    "type": "object",
    "required": [
      "element"
    ],
    "additionalProperties": false,
    "properties": {
      "element": {
        "type": "string"
      }
    }
  }
}